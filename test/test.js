var chai = require('chai'),
    mocha = require('mocha'),
    assert = require('assert'),
    should = chai.should();
    
/* global describe, it */
 
describe("echo", function () {
    it("should work", function(done) {
        done();
    });
});